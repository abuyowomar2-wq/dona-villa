# Database URL (SQLite for local dev, PostgreSQL for production)
DATABASE_URL="file:./dev.db"

# For PostgreSQL (Render, Railway, etc.):
# DATABASE_URL="postgresql://user:password@host:5432/donavilla?schema=public"

# Admin session secret (change in production!)
NEXTAUTH_SECRET="change-this-to-a-random-secret"
